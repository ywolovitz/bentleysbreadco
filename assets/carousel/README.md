# Carousel Images Directory

Add your images here (jpg, png, gif, or webp format).

After adding images, run from the project root:
```bash
node generate-carousel.js
```

This will automatically update the carousel with your new images.
